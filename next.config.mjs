/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    turbo: {
      root: '/Users/unknownfriend/607pm-website'
    }
  }
};

export default nextConfig;
